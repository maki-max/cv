var numberOfDrumButtons = document.querySelectorAll(".btn").length;
for (var i = 0; i < numberOfDrumButtons; i++) {
    document.querySelectorAll(".btn")[0].addEventListener("click", result)
  
  function result(){
    document.body.innerHTML="";
  }
   
}
